package extra

const (
	ScaleBy = iota // Multiplier to width and height of the original image
	ScaleTo        // Scale to specify width and height

	SCALE_BY = ScaleBy // Multiplier to width and height of the original image
	SCALE_TO = ScaleTo // Scale to specify width and height
)
